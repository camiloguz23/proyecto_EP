/* const documento = document.getElementById("usuario")
const clave = document.getElementById("password");
const form = document.getElementById("form1");
const aviso = document.getElementById("aviso");

/* form.addEventListener("submit", e=>{
    e.preventDefault()
    let warnings = ""
    let entrar = false
    if(documento.value.length < 6){
       warnings += `El documento no es valido <br>`
       entrar = true
    }
    if(clave.value.length < 10){
        e.preventDefault()
        warnings += `la contrasña tiene que tenerminimo 10 caracteres <br>`
        entrar = true
    }
    
    if(entrar){
        e.preventDefault()
        aviso.innerHTML=warnings
    }
})
 */ 

 





